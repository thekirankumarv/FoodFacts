/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.openfoodfacts.org'], // Allow images from this domain
    },
  };
  
  export default nextConfig;
  